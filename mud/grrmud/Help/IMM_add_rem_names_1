<help olc add_oname>

There are several commands that let you add names to
a mobile or object.  The syntax is the same for all of them:

Syntax:  command [obj or mob num] [name to be added]

Example:  add_oname 55 cup
          add_mname 45 guard

The commands are:
add_oname, add_mname
<color=red>
See Also:  clear_oname, clear_mname, ch_rname, ch_odesc
</color></help>
